# DECISIONS.md — rulings made after the authority docs were frozen
*Log only. `/docs` is never edited; conflicts and open facts get resolved here with Pragaman's explicit yes.*

| Date | Ref | Ruling | Source |
|---|---|---|---|
| 23 Aug 2026 | PRD §7 unresolved fact | **Buddy Software departments = 7.** W19's count stands everywhere (hero stat `7 DEPTS → 1 PLATFORM`, before-picture "7 sheets + 1 master"). W5's six director functions (GST, Taxation, Accounting, Finance, Compliance, Sales) remain the description of who was interviewed. | Pragaman, this session |
| 23 Aug 2026 | Repo layout | Planning docs moved from repo root into `/docs` per CLAUDE.md; superseded versions (PRD v0.1, handoff v1, specimen v2 HTML) into `/docs/archive`. `ME.pdf` = The Pragaman Files questionnaire (raw W/P/S answers). | Pragaman approved Phase 0 kickoff incl. reorg |
| 23 Aug 2026 | Missing asset | ~~`FB_1_year_Work_Report.pdf` not yet in repo~~ **Resolved same day** — Pragaman supplied it; verified against facts.json, renamed to canonical name, committed. | Pragaman |
| 23 Aug 2026 | Repo visibility | **GitHub repo (Pragaman07/Website) is PUBLIC by Pragaman's explicit choice**, made with full knowledge that `/docs` (ME.pdf raw questionnaire incl. boss's name, handoff docs incl. role-transition intent) is publicly readable. Was offered private / docs-excluded alternatives; chose public with everything in. Do not re-ask. | Pragaman, in chat |
| 23 Aug 2026 | §13 vs §6.1/§1.2 contrast conflict | Lighthouse proved the spec's coral assumptions miss WCAG AA: `#FF6B5E` on `#FAFAF7` = 2.67:1 (< 3.0 even for large text); white on coral = 2.79:1; `#E85546` small text = 3.45:1 (< 4.5). §13 declares contrast **launch-blocking**, so it wins. Rendering rulings: large coral heading accents → `--accent-deep` (3.6:1 ✓ large-bold); small coral links → **ink text + 2px coral underline**, hover accent-deep; primary button → **ink text on coral** (5.8:1, any size); table change columns + verdict labels → ink bold (coral dot carries the color). Coral-on-ink (bedrock band, results strip) passes as-is. **Pragaman may veto with a palette amendment**; until then these hold. | Claude, per §13 launch-blocking rule — awaiting Pragaman's ack |
| 23 Aug 2026 | Font loading | Bricolage loads as static 700/800 slices, not full variable+opsz (Foundations v1 specifies only those weights; the variable file pushed hero LCP to 3.1s on 4G). Caveat is `preload:false` (Know-Me-only — Work pages don't pay for it). | Claude, perf gate — implementation detail |
| 24 Aug 2026 | §13 contrast/legibility, round 2 | Know-mode `--muted` darkens to `#616A76` (Work keeps `#6B7280`): the shared value is 4.44:1 on cream — a hair under AA; the darker one clears 4.9:1, visually near-identical. `mono-label` token 12px (spec §1.2 said 11px): Lighthouse's legibility floor inside the §13 ≥95 gate flags 11px-majority pages. Same §13-wins precedent as the coral rulings — **Pragaman may veto**. | Claude, per §13 — awaiting Pragaman's ack |
| 24 Aug 2026 | §13 contrast, round 3 (PageSpeed on prod) | ResultsStrip source line was `--muted` (#6B7280) on the ink card — 3.7:1, fails AA at mono-label size; PageSpeed desktop run on /work/filing-buddy scored a11y 96 (local mobile-emulated runs missed it — strip mid-reveal when axe scanned). New token `--muted-on-ink: #9CA3AF` (7:1 on ink, still reads secondary) used for on-ink secondary text; same §13-wins precedent — **Pragaman may veto**. | Claude, per §13 — awaiting Pragaman's ack |
| 23 Aug 2026 | India map depiction (retro-logged 25 Aug) | **Full-Kashmir Indian (Survey-of-India) depiction** via @svg-maps/india (CC BY 4.0, credited in the map figcaption), verified by rasterizing with sharp. City coords calibrated in `lib/india.ts`. | Pragaman-approved during Phase 4 |
| 25 Aug 2026 | Verification cadence | **No routine Lighthouse/PageSpeed re-measuring.** The perf gate was measured green on prod 24 Aug (all routes ≥95 both form factors); fix later only if something visibly drifts. | Pragaman, in chat |
| 25 Aug 2026 | Placeholder build | **Complete lorem-placeholder build approved for production.** Every section built + populated with clearly-fake lorem (draft-flagged/note-tracked); real content replaces it in the Content-Doc pass. facts.json stays untouched — no invented numbers in the receipts registry. | Pragaman, in chat ("populate with lorem ipsum, complete build of all sections, all features working") |
| 25 Aug 2026 | Memoji v1 spots | Pragaman asked for v1 memoji proposals; Claude proposed + shipped three: **hub polaroid corner sticker** (derived `memoji-sticker.png`), **404 plot mourner**, **Know-footer scribble buddy**. Skipped as noise: level-tile hover faces, changelog header. `memoji-full`/`memoji-call` stay reserved (egg 8 / contact area). **Pragaman may veto/add.** | Pragaman asked; Claude placed — veto open |
| 23 Aug 2026 | **Chai → COFFEE, sitewide** | Pragaman is a coffee person (this also answers P19). Every chai reference becomes coffee: egg 4 = the coffee counter (`COFFEE CONSUMED (GLOBAL, LIVE)`), KV key `counter:coffee`, route `/api/coffee`, sound id `coffee-sip` (file stays `sip.wav`), footer sign-off draft, future `CoffeeCounter` component. PRD §6/Design Spec §12.4 still say chai — this ruling wins. Renamed before any production KV data existed, so no migration. | Pragaman, in chat |
| 3 Sep 2026 | **Revamp brief — scope** | After the placeholder build, Pragaman opened a revamp ("more personal and genuine, the site feels without life"): night mode · number font · refined icons · memoji life · card-style case studies · centred pitch block · a fuller footer · real motion. Two SaaS screenshots (3-up pricing cards; centred CTA on a warm gradient) are **layout inspiration for those two blocks only**, rendered in the site's own tokens (Bricolage, coral, 18px radius). One addition from them: a faint coral wash (≤8% coral into `--bg`) behind the cards and pitch sections — the only gradient on the site; the §6.1 hero stays flat. | Pragaman, in chat (Q1 → Claude's recommendation) |
| 3 Sep 2026 | **Rule 5 / Foundations §1 — Space Mono retired** | Pragaman: the number font "feels out of place". New rule: **display-scale numbers (hero stats, counters, card figures) wear Bricolage Grotesque; tabular data, labels, version tags and timestamps wear Geist Mono.** `--font-mono` = Geist Mono; `.type-mono-stat(-xl)` now set the display face (class names kept — 40+ call sites); new `.type-data` for table cells / version chips / error chrome. | Pragaman (Q2 "your suggestion" → option 1) |
| 3 Sep 2026 | **Night mode** (new — no authority doc covers it) | Second axis `data-theme="light\|dark"` on `<html>`, orthogonal to `data-mode`. Resolves from `localStorage("pragaman-theme")` if set, else `prefers-color-scheme`; the boot script stamps it before paint; the OS is followed live until the visitor overrides. **Both modes get a dark palette** (values in `globals.css`, derived by Claude and contrast-checked: text ≥ 4.5:1, large coral ≥ 3:1 — **awaiting Pragaman's ack** like the earlier contrast rounds). New semantic tokens so the dark theme works without per-component hacks: `--band/--on-band/--muted-on-band` (the emphasis band — ink in light, a raised surface in dark; replaces `--muted-on-ink` from round 3), `--on-accent` (text on coral: ink in both themes), `--scrim`. Dark `--accent-deep` goes *lighter* (#FF8F84) — "small-text-safe coral" is a contrast role, not a hue. Control: sun/moon button beside mute in the header on ≥md; below md, mute + theme move into the nav panel; repeated in the footer. No sound on a theme change (rule 8's list is closed). | Pragaman (Q3 A, Q4 → recommendation) |
| 3 Sep 2026 | §3.2 toggle faces | **Surprise memoji = Work resting face; wink memoji = Know Me resting face.** The real headshot leaves the toggle (it stays on the Know Me polaroid). Claude flagged that the photo-vs-cartoon crossfade was the "same person, two temperatures" joke in miniature; Pragaman chose two memojis. | Pragaman (Q6 A) |
| 3 Sep 2026 | Memoji placements v2 (memoji-full / memoji-call unreserved) | memoji-call → the pitch block · Heart → Intake success state + Know Me footer beside the scribble · Hands on Waist → Work hero, right side (§6.1 "no photo in hero" deviation — a memoji, not a photo; transparent asset still to be supplied) · memoji-full → the Door's "The Person" half · Surprise → toggle + egg 1's error card. Egg 8 (living memoji) stays post-v1 until sleeping/annoyed frames exist. | Pragaman (Q7) |
| 3 Sep 2026 | §6.2 case cards | Vertical row stack replaced by **3-up cards on desktop, snap-scroll swipe rail on mobile**; anatomy tag → title → hook → hero stat → three ✓ receipts (facts.json only) → link; Filing Buddy raised as the featured card. | Pragaman (Q8) |
| 3 Sep 2026 | §10 pitch block | The Intake gets a **centred, full-width lead block** — title, sub, one pill CTA, the live `Problems pitched · Replies sent` counter as the reassurance line, memoji-call beside it; the 5-step card expands inline below on CTA click (no modal). CTA stays coral; pill radius on this CTA only. | Pragaman (Q9 "centre align") |
| 3 Sep 2026 | §2.3 footer | Extended anatomy: wordmark + one-liner · per-mode nav columns · contact block (email, calendar, socials) + the live counters · bottom bar (sign-off, mute + theme, ©, build stamp `v{age}.{month}`, map credit). Socials supplied so far: Instagram `instagram.com/prag.a.man`, LinkedIn `linkedin.com/in/pragaman-kumar-anurag` (tracking params stripped); email / calendar / others to come. | Pragaman (Q10) |
| 3 Sep 2026 | §5 / D-7 motion budget | D-7 amended: the flip stays the signature, but the site gains page-load choreography (hero words, count-ups, memoji spring-in), micro-interactions (magnetic CTA, card tilt, press squash) and a small dose of ambient motion (memoji bob, sticker drift, wash shift). Route transitions optional; scroll-driven parallax/pinning stays banned. Every new motion ships with its reduced-motion fallback (rule 10). | Pragaman (Q11 → recommendation) |
| 3 Sep 2026 | Icons | Level-tile emoji (🪦 📊 🗺 📌 ❓ — the spec's own "final glyphs in build" placeholders) and the coffee/receipt/call marks → custom SVG glyphs in the site's line grammar (1.5px ink stroke, coral accent); lucide stays for chrome (menu, ×, mute, sun/moon). | Claude's recommendation — Q12 went unanswered (Pragaman used it for links); veto open until chunk 3 |
